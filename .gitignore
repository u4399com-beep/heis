# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
.z-ai-config
*.log
dev.log
dev.out.log
/test
/prompt

server.log
# Skills directory
/skills/tool-results/
/tool-results/
/tmp/

# python venv (旧 mini-services/scrapling-bridge, uc-bridge, curl-impersonate-bridge,
# trafilatura-bridge 已在 R43-1C 全删; 规则保留兜底, 防止未来 Python 内容混入)
mini-services/**/.venv/
.venv/

# runtime data (jj-f 生产就绪: 运行时数据库与封面不进版本库)
db/*.db
db/*.db.lost-*
db/*.db-journal
/data/
backups/
tmp/

# rr 轮: 验证截图目录(42 PNG 7.3MB 曾误入库)
tmp-shots/

# tt 轮: .env.example 模板保留入库(.env* 规则的例外)
!.env.example

# R30: 临时文件
.zscripts/
__pycache__/
*.pyc
*.pid

# R41-1C: Go 后端构建产物 — 9 个二进制(heis-backend + 8 个 services 子项目)及临时日志
# 均为 `go build` 输出, 源码在 ./*.go 与 ./services/*/main.go; 不入版本库(原 9 个二进制共
# ~100MB 已被 git 跟踪, 后续 git rm --cached 解除跟踪)
# R42-1C: 9 个二进制 + backend.log 已从工作目录删除(总计 ~100MB), 需用时 go build 重新生成.
#         scripts/archive/ + agent-ctx/archive-pre-r28/ 同步清理(过时归档).
# R43-1C: 旧 Python/bun mini-services/* 全删, Go 版本在 go-backend/services/* (11 个).
#         start-all.sh 改为构建并启动 go-backend/bin/<svc> 二进制 (端口 3010-3020).
#         3 个 A agent 新增服务 (curl-impersonate-bridge / trafilatura-bridge / cloak-browser)
#         已在 services/ 目录, 启动方式与其余 8 个一致.
go-backend/heis-backend
go-backend/qimao-proxy
go-backend/deqixs-proxy
go-backend/fetch-relay
go-backend/scrapling-bridge
go-backend/xjp-proxy
go-backend/uc-bridge
go-backend/bqg713-proxy
go-backend/moli-bridge
go-backend/curl-impersonate-bridge
go-backend/trafilatura-bridge
go-backend/cloak-browser
# 兜底: 未来新增的 services/* 子项目二进制也按此模式命名(Port + service name)落在根目录
go-backend/*-proxy
go-backend/*-bridge
go-backend/*-backend
go-backend/*-browser
# start-all.sh 构建输出目录(11 个 Go 服务二进制, 每个约 10MB, 不入版本库)
go-backend/bin/
go-backend/services/*/*
!go-backend/services/*/*.go
!go-backend/services/*/go.mod
!go-backend/services/*/go.sum
!go-backend/services/*/scripts/
!go-backend/services/*/.env.example
# 显式覆盖 *.log 通用规则已存在, 此处补充 go-backend/ 路径注释明确意图
go-backend/*.log

