# uc-bridge (agent-JJ-turnstile) Python 依赖
# 安装: cd mini-services/uc-bridge && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# 或全局: pip install -r requirements.txt

# undetected-chromedriver: patches Chromium 规避 CDP 检测 / navigator.webdriver 等自动化指纹
undetected-chromedriver>=3.5.5

# selenium: undetected-chromedriver 底层依赖(显式约束版本下限, 避免 UC 与 selenium 版本错配)
selenium>=4.15.0

# pyvirtualdisplay: L3 级 xvfb 包装(无 DISPLAY 服务器场景必备; 包装 Xvfb 子进程)
# 仅在 DISPLAY 未设或 ?xvfb=1 时启用; 不可用时 L3 自动降级 L4
pyvirtualdisplay>=3.0
